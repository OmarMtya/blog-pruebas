import { PostTimerPipe } from './post-timer.pipe';

describe('PostTimerPipe', () => {
  it('create an instance', () => {
    const pipe = new PostTimerPipe();
    expect(pipe).toBeTruthy();
  });
});
